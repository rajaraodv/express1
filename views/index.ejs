<!DOCTYPE HTML>
<html>
<head>
    <title> <%= title %> </title>
    <link rel='stylesheet' type='text/css' href='/stylesheets/bootstrap.min.css'>
</head>
<body>
<div class='container'>
    <table align='center'>
        <tr>
            <td>
                    <form class="form-horizontal well" method="POST" action="/login">
                        <fieldset>
                            <legend>ExpressJS Session & Routing demo</legend>
                            <fieldset>
                                <p>To login enter: <code>user1/test123 or user2/test123</code> as username/password.</p>

                                <p>If you enter <strong>wrong</strong> username/password more than 3 times, it should
                                    block login.</p>
                            </fieldset>
                            <fieldset>
                                <div class="control-group error">
                                    <label><strong> <%= flash %> </strong></label>
                                </div>
                            </fieldset>
                            <div class="control-group">
                                <label class="control-label" for="user">Username:</label>

                                <div class="controls">
                                    <input type="text" class="input-xlarge" id="user" name="user" value=<%= user %>  ></input>
                                </div>
                            </div>

                            <div class="control-group">
                                <label class="control-label" for="password">Password:</label>

                                <div class="controls">
                                    <input type="password" class="input-xlarge" id="password" name="password"></input>
                                </div>
                            </div>

                            <div class="control-group">
                                <div class="controls">
                                    <input type="submit" class="btn btn-primary" id="submit" value="Login"></input>
                                </div>
                            </div>
                        </fieldset>
							<h4>Note:</h>	
							<br>
                            <code>1. This page was served from: <%= hostAndPort %></code>
							<br>
							<br>
<code>2. Nginx sticky session (Cookie 'jsessionid') is: <strong><%= stickySession %> </strong></code>
						
                    </form>
            </td>
        </tr>
    </table>
</div>
</body>
<script type="text/javascript" src="../javascripts/jquery-1.7.2.min.js"></script>
<script>
    $(function() {
		$("#user").focus();
    });
</script>
</html> 

